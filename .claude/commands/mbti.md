---
description: 지금까지 누적된 성향을 패싯 단위로 보여주고, 명언 한 줄과 해볼 일을 제안한다
argument-hint: (없음) | 갱신 | 패싯 | 규칙
---

`.claude/skills/mbti/SKILL.md` 파일을 먼저 전부 읽는다. 그 다음 `Experiences/성향 누적.md` 를 읽고, 그 문서의 규칙을 그대로 따라 **$ARGUMENTS** 에 맞게 응답한다.

빈 축을 추정으로 채우지 않는다. 출처를 댈 수 없는 인용은 쓰지 않는다.
